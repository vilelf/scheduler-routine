const TIMES = [];
for (let i = 6; i < 24; i++) {
  TIMES.push(`${i}:00`)
  TIMES.push(`${i}:30`)
}
const DAYSOFWEEK = ['', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday', 'Sunday'];

export { TIMES, DAYSOFWEEK };