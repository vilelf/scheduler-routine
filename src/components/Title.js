import React from 'react';
import styled from 'styled-components';

const TitleText = styled.h1`
    text-align: center;
`;

const Title = () => {
    return (
        <TitleText>Week Scheduler</TitleText>
    )
}

export default Title;